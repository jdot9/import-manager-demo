import { AxiosPromise } from '../types';
type SessionInitiationResponse = {
    id: string;
    token: string;
    createdAt: number;
    updatedAt: number;
    expiresAt: number;
    tokenStatus: "NEW" | "REFRESHED";
};
export declare function initiateSession(accountId: number, resetSession?: boolean): AxiosPromise<SessionInitiationResponse>;
export declare function diffDeveloperFileHashes(accountId: number, token: string, fileHashes: Record<string, string>): Promise<import("axios").AxiosResponse<any, any, {}>>;
export {};
