"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.initiateSession = initiateSession;
exports.diffDeveloperFileHashes = diffDeveloperFileHashes;
const http_1 = require("@hubspot/local-dev-lib/http");
const PREVIEW_API_PATH = `local-preview/v1`;
async function initiateSession(accountId, resetSession = false) {
    return http_1.http.post(accountId, {
        url: `${PREVIEW_API_PATH}/session/create`,
        headers: {
            "Content-Type": "application/json"
        },
        data: {
            resetSession
        }
    });
}
async function diffDeveloperFileHashes(accountId, token, fileHashes) {
    return http_1.http.post(accountId, {
        url: `${PREVIEW_API_PATH}/developer-file-hashes/diff`,
        headers: {
            "Content-Type": "application/json"
        },
        data: {
            token,
            fileHashes
        }
    });
}
