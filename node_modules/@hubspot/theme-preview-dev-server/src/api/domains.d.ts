import { AxiosPromise } from '../types';
type FetchDomainsResponse = {
    results: Array<string>;
};
export declare function fetchDomains(accountId: number): AxiosPromise<FetchDomainsResponse>;
export {};
